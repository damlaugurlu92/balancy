// SADECE GELİŞTİRME İÇİN — production'da kullanma!
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /transactions/{txId} {
      allow read, write: if true;
    }
  }
}
